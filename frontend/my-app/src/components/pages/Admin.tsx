import { FC } from "react";
import { DefaultLayout } from "../templates/DefaultLayout";

export const Admin: FC = () => {
  return (
    <div>
      <DefaultLayout>
        <h2>管理画面</h2>
      </DefaultLayout>
    </div>
  );
};
