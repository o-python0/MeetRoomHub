import { FC } from "react";
import { DefaultLayout } from "../templates/DefaultLayout";

export const Settings: FC = () => {
  return (
    <div>
      <DefaultLayout>
        <h2>設定画面</h2>
      </DefaultLayout>
    </div>
  );
};
